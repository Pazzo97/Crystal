<nav class="navbar navbar-expand-lg bg-light nav-back">
  <div class="container-fluid">
    <div class="navbar-brand"> 
        <strong><a class="navbar-brand app-header" href="/">Crystal Budget</a></strong>
    </div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <% if user_signed_in? %>
            <li class="nav-item mt-2">
              <a class="nav-link categories-text" href="<%= categories_path %>">Categories</a>
            </li>
            <li class="nav-item mt-2">
              <%= button_to(
                    "Log Out",
                    destroy_user_session_path,
                    method: :delete,
                    class: 'btn btn-danger'
                  ) %>
            </li>
          <% else %>
            <li class="nav-item mt-2 me-5">
              <a class="btn btn-success" href="<%= new_user_session_path %>">Login</a>
            </li>
            <li class="nav-item mt-2 me-5">
              <a class="btn btn-info" href="<%= new_user_registration_path %>">Register</a>
            </li>
          <% end %>
      </ul>
    </div>
  </div>
</nav>
